<%= form_for(@blog, url: choose_new_or_edit) do |f| %>
        <% if @blog.errors.any? %>
            <div id="error_explanation_c">
                <h2><%= @blog.errors.count%>件のエラーがあります</h2>
                <ul>
                <% @blog.errors.full_messages.each do |mes|%>
                    <li><%= mes %></li>
                <% end %>
                </ul>
            </div>
        <% end %>
               
        <%= f.label :content %>
        <%= f.text_field :content, class: 'form-control form-control-lg', placeholder: "What do you now?" %>
        
        <br>
        <%= f.submit "投稿", class: 'btn btn-primary form_button'%>
        <br>
        <br>
        <br>
        <%= link_to "一覧に戻る", blogs_path, class: 'form_button' %>
  
<% end %>